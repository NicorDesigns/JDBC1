ALTER TABLE CHARITYDB.`CHARITY` DROP PRIMARY KEY;

DROP TABLE CHARITYDB.`CHARITY`;

CREATE TABLE CHARITYDB.`CHARITY` (
	`CHARITY_ID` MEDIUMINT NOT NULL AUTO_INCREMENT,
	`CHARITY_TAX_ID` VARCHAR(250) NOT NULL, -- https://tax.co.za/what-is-my-tax-number/
	`CHARITY_NAME` VARCHAR(250),
	`CHARITY_MISSION` VARCHAR(250),
 --	`CHARITY_CATEGORY` VARCHAR(45), -- Need a separate Category Table
	`CHARITY_PROGRAMS` VARCHAR(250), -- Need a separate PROGRAMS Table
 -- `CHARITY_ADDRESS` VARCHAR(45), --Need a separate ADDRESS Table
    `CHARITY_WEB_ADDRESS` VARCHAR(45), -- Need a separate ADDRESS Table
    `CHARITY_FACEBOOK_ADDRESS` VARCHAR(45), -- Need a separate ADDRESS Table
    `CHARITY_TWITTER_ADDRESS` VARCHAR(45), -- Need a separate ADDRESS Table
    PRIMARY KEY (`CHARITY_ID`)
) ENGINE=InnoDB;

CREATE UNIQUE INDEX `PK_CHARITY` ON CHARITYDB.`CHARITY` (`CHARITY_ID` ASC);
