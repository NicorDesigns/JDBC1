ALTER TABLE CHARITYDB.`CHARITY_CATEGORY` DROP INDEX `IDX_CHARITY_CATEGORY`;

DROP TABLE CHARITYDB.`CHARITY_CATEGORY`;

CREATE TABLE CHARITYDB.`CHARITY_CATEGORY` (
		`CHARITY_CATEGORY_ID` MEDIUMINT NOT NULL AUTO_INCREMENT,
        `CHARITY_ID` MEDIUMINT NOT NULL, -- Foreign Key
        `CHARITY_CATEGORY_NAME` VARCHAR(50),
		`CHARITY_CATEGORY_CODE` VARCHAR(25), -- Possible Tax Code
		CONSTRAINT `CHARITY_CATEGORY_PK` PRIMARY KEY (`CHARITY_CATEGORY_ID`)
	);

CREATE UNIQUE INDEX `IDX_CHARITY_CATEGORY` ON CHARITYDB.`CHARITY_CATEGORY` (`CHARITY_CATEGORY_ID` ASC);

ALTER TABLE `CHARITY_CATEGORY` ADD CONSTRAINT `FK_CHARITY_ID_CATEGORY` FOREIGN KEY (`CHARITY_ID`)
    REFERENCES `CHARITY` (`CHARITY_ID`) #
    ON DELETE RESTRICT
    ON UPDATE CASCADE;


